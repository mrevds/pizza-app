version: "3"

dotenv: [".env"]

tasks:
  install-deps:
    cmds:
      - go install google.golang.org/protobuf/cmd/protoc-gen-go@v1.28.1
      - go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@v1.2
      - go install github.com/pressly/goose/v3/cmd/goose@v3.14.0
    env:
      GOBIN: "{{.PWD}}/bin"

  generate:
    cmds:
      - task: generate-user-api

  generate-user-api:
    cmds:
      - mkdir -p pkg/user-service_v1
      - protoc --proto_path api --go_out=pkg/user-service_v1 --go_opt=paths=source_relative --plugin=protoc-gen-go=bin/protoc-gen-go.exe --go-grpc_out=pkg/user-service_v1 --go-grpc_opt=paths=source_relative --plugin=protoc-gen-go-grpc=bin/protoc-gen-go-grpc.exe api/user-service_v1/user.proto

  migrate-up:
    cmds:
      - ./bin/goose.exe -dir {{.MIGRATION_DIR}} postgres "host=localhost port={{.PG_PORT}} dbname={{.PG_DATABASE_NAME}} user={{.PG_USER}} password={{.PG_PASSWORD}} sslmode=disable" up -v
